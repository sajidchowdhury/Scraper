{
  "name": "gmaps-scraper",
  "version": "0.4.0",
  "description": "Google Maps business scraper — Phase 1 (search, paginate, extract, export).",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "bun test tests/",
    "syntax": "node --check src/index.js && node --check src/extract.js && node --check src/scroll.js && node --check src/search.js && node --check src/browser.js && node --check src/config.js && node --check src/logger.js"
  },
  "dependencies": {},
  "license": "MIT"
}
