{
  "name": "gmaps-scraper",
  "version": "0.6.0",
  "description": "Google Maps business scraper — Phase 1 (search, paginate, extract, deep scrape, CSV export).",
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "scripts": {
    "start": "node src/index.js",
    "test": "bun test tests/",
    "syntax": "node --check src/index.js && node --check src/extract.js && node --check src/detail.js && node --check src/export.js && node --check src/scroll.js && node --check src/search.js && node --check src/browser.js && node --check src/config.js && node --check src/logger.js"
  },
  "type": "commonjs"
}
